touch heart.db

sudo chmod 777 heart.db

sqlite3

.open heart.db

CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    heart_rate REAL,
    chest_volume INTEGER,
    blood_oxygen_concentration REAL,
    producer_timestamp TIMESTAMP,
    topic_entry_timestamp TIMESTAMP,
    topic_exit_timestamp TIMESTAMP,
    consumer_timestamp TIMESTAMP
);
